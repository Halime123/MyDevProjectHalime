Jenkins:
what are servers?
1)Stay power on all the time.
2)Strong internet connection(in GBps)all the time.
3)A lot of resources like RAM Disk etc
===============================================
1)Developers will develops the code
2) Runs the unit tests if everything goes well ask the DevOPs person to build that code
and deploy the code on DEV servers
3)Developers verifies that code on Dev server
4)Request to DevOps person to deploy it on the integration server
5)Tester will ask DevOps person to code QA server
6)Tester may deploy it on the load server on Performance server,  Security testing servers
7) DevOps person deploy application staging servers(replica of production server) UAT server on which UAT is performed.
8)to production server
Issues in the manual Testing/deployment

Issues in manual Testing/deployment
1)Time consuming
2) If multiple developers are working there can be issues in build and
to fix those issues we may need to look all of the newly added code
3)No automation means a lot of manual effort


http://jenkins.syntaxtechs.net:8080/job/Halime%20Project%201/1/console
username admin
password admin
scroll down click on build steps and write acho"any msg"
click save and apply to console your project



